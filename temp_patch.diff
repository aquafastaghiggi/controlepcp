*** Begin Patch
*** Update File: assets/js/app.js
@@
-    const baseInicio = safeCell(data.prg_base_inicio);
-    const eficiencia = safeCell(data.prg_eficiencia);
@@
-    const novaJanela = window.open('', '_blank');
+    const firstItem = (data.schedule || [])[0];
+
+    function formatDateBR(data) {
+      if (!data) return '';
+      const [y, m, d] = String(data).split('-');
+      if (!y || !m || !d) return '';
+      return ${d}//;
+    }
+
+    function formatHora(hora) {
+      if (!hora) return '';
+      return String(hora).slice(0, 5);
+    }
+
+    const inicioBaseFormatado = firstItem
+      ? ${formatDateBR(firstItem.sch_data_inicio)} 
+      : '';
+
+    const novaJanela = window.open('', '_blank');
@@
-          <div class= preview-meta>
-            <span><strong>Lote:</strong> </span>
-            <span><strong>Início base:</strong> </span>
-            <span><strong>Eficiência:</strong> </span>
-          </div>
+          <div class=report-header-meta>
+            <span><strong>Linha:</strong> </span>
+            <span><strong>Início base:</strong> </span>
+            <span><strong>Eficiência:</strong> %</span>
+          </div>
*** End Patch
